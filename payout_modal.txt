      {/* Payout Modal (Matched to Screenshot) */}
      {payoutModalTrainer && (
        <div className="fixed inset-0 bg-[#020617]/80 backdrop-blur-sm flex items-center justify-center p-4 z-50">
          <div className="bg-[#111827] rounded-2xl p-6 max-w-lg w-full border border-slate-800 shadow-2xl relative" dir="rtl">
            {/* Close Button */}
            <button
              onClick={() => setPayoutModalTrainer(null)}
              className="absolute top-4 left-4 w-8 h-8 flex items-center justify-center rounded-lg hover:bg-slate-800 text-slate-400 hover:text-slate-200 transition-colors"
            >
              <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M18 6 6 18"/><path d="m6 6 12 12"/></svg>
            </button>

            {/* Header */}
            <div className="flex items-center justify-between mb-6">
              <h2 className="text-base font-bold text-slate-200 flex items-center gap-2">
                سند صرف مستحقات تدريبية
              </h2>
              <div className="text-emerald-500 font-bold">
                 $
              </div>
            </div>

            <form onSubmit={handlePayoutSubmit} className="space-y-4">
              {/* Info Box */}
              <div className="bg-[#1e293b] p-4 rounded-xl border border-slate-700/60 text-sm">
                <div className="flex justify-between items-center mb-1">
                  <span className="text-slate-200 font-bold text-sm">المدرب: {payoutModalTrainer.name}</span>
                </div>
                <div className="text-[11px] text-slate-400 flex items-center gap-2">
                  <span>إجمالي المستحق له: <span className="font-bold text-slate-300">{payoutModalTrainer.totalDues}</span> ع.م | المتبقي للصرف: <span className="font-bold text-amber-500">{payoutModalTrainer.remainingDues} ج.م</span></span>
                </div>
              </div>

              {/* Amount Input */}
              <div className="space-y-1.5">
                <label className="block text-[11px] font-bold text-emerald-500">
                  المبلغ المصروف الآن (ج.م) *
                </label>
                <input
                  type="number"
                  value={payoutAmount}
                  onChange={(e) => setPayoutAmount(e.target.value)}
                  className="w-full p-2.5 bg-[#0b101e] border border-emerald-500/50 rounded-lg text-sm font-bold text-white focus:outline-none focus:border-emerald-400 focus:ring-1 focus:ring-emerald-500/50 text-right"
                  placeholder="1000"
                  required
                />
              </div>

              {/* Payment Method */}
              <div className="space-y-1.5">
                <label className="block text-[11px] font-bold text-slate-300">
                  طريقة الصرف
                </label>
                <select className="w-full p-2.5 bg-[#1e293b] border border-slate-700 rounded-lg text-sm font-bold text-slate-200 focus:outline-none focus:border-slate-500">
                  <option value="cash">نقداً من خزينة المركز</option>
                  <option value="vodafone">فودافون كاش</option>
                  <option value="instapay">انستاباي InstaPay</option>
                  <option value="bank">تحويل بنكي</option>
                </select>
              </div>

              {/* Notes */}
              <div className="space-y-1.5">
                <label className="block text-[11px] font-bold text-slate-400">
                  البيان / الملاحظات
                </label>
                <input
                  type="text"
                  defaultValue={`صرف مستحقات تدريبية للمدرب ${payoutModalTrainer.name}`}
                  className="w-full p-2.5 bg-[#1e293b] border border-slate-700 rounded-lg text-xs text-slate-300 focus:outline-none focus:border-slate-500"
                />
              </div>

              {/* Action Buttons */}
              <div className="flex items-center gap-3 pt-2">
                <button
                  type="submit"
                  className="px-6 py-2 bg-emerald-600 hover:bg-emerald-500 text-white rounded-lg text-xs font-bold transition-colors cursor-pointer shadow-lg shadow-emerald-900/20"
                >
                  تأكيد الصرف وحفظ السند
                </button>
                <button
                  type="button"
                  onClick={() => setPayoutModalTrainer(null)}
                  className="px-6 py-2 bg-[#1e293b] hover:bg-slate-700 text-slate-300 rounded-lg text-xs font-bold transition-colors cursor-pointer"
                >
                  إلغاء
                </button>
              </div>
            </form>
          </div>
        </div>
      )}
